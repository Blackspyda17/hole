<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.motivaimagine.motivaimagine_trial">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".MotivaUSTrialApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        >
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme_Norm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/Login"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".CreateAccountActivity"
            android:label="@string/Register"
            android:parentActivityName=".doctor_list"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".Main2Activity"
            android:label="@string/Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".doctor_list"
            android:label="Select Yout Doctor"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme"/>


        <activity android:name=".forms_menu" />
        <activity android:name=".forgot_pass"
                  android:label="@string/recover_your_password"
                  android:parentActivityName=".LoginActivity"
                  android:theme="@style/AppTheme"/>

        <activity android:name=".Main_modules" />
        <activity android:name=".modules" />
        <activity android:name=".forms_atep_1"></activity>
    </application>

</manifest>